hx711(weight, D6, D7, 0, true); # the 0 is weight calibration, as the scale defaults to something inaccurate to the real world. The true at the end means that the topic to which +/-delta can be sent to change the calibration
#Didn't have the exact code, as the girls' group did the scale and forgot to send us the code.
